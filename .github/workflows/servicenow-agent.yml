name: ServiceNow Agent

on:
  workflow_dispatch:
    inputs:
      command:
        description: "Tell the agent what to do"
        required: true
        type: string

jobs:
  run-agent:
    runs-on: ubuntu-22.04
    steps:
      - name: Print command
        run: echo "Command is ${{ github.event.inputs.command }}"